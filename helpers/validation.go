package helpers

func Validator(s interface{}) {
	// val := validator.New()
	// err := val.Struct(s)

	// for _, e := range err.(validator.ValidationErrors) {

	// errResult := make(map[string]interface{})
	// errResult[e.StructField()] = e.Error()
	// }
}
